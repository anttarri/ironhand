import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // PWA and offline support via service worker will be handled client-side
  // The manifest.json is served from /public
};

export default nextConfig;
